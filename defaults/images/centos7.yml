---
# Centos 7 image

"docker_provisioner_centos:7":
  dockerfile: Dockerfile.centos_7
  buildargs:
    user: "{{ docker_provisioner_container_options.ansible_user }}"
    password: "{{ docker_provisioner_container_options.ansible_password }}"
  path: "{{ role_path }}/files"
